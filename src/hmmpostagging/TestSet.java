package hmmpostagging;

import java.io.File;

/**
 * The DataSet used for Tests. Clearly does not need its own class at this
 * point, but in case it needs it for the future...
 * 
 * @author Taylor
 */
public class TestSet extends DataSet {
    public TestSet(File file) {
        super(file);
    }
}
